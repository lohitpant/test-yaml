name: Variables and Context

on:
  workflow_dispatch:
    inputs:
      run-name-job:
        description: "Person to Greet"
        default: "World"
        required: true

jobs:
  job1:
    name: "Output GITHUB_CONTEXT"
    runs-on: ubutu-latest
    steps:
      - name: Dump Github Context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
